============
Installation
============

At the command line::

    $ easy_install django-discussion

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-discussion
    $ pip install django-discussion
